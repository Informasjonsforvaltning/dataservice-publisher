version: '3'
services:
  dataservice-publisher:
    build: ../.
    image: eu.gcr.io/digdir-fdk-infra/dataservice-publisher
    environment:
      - HOST_URL=${HOST_URL}
      - HOST_PORT=${HOST_PORT}
      - PUBLISHER_URL=${PUBLISHER_URL}
    ports:
      - "8080:8080"
